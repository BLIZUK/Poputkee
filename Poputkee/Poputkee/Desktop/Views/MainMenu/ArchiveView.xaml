<UserControl x:Class="Poputkee.Desktop.Views.MainMenu.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" 
             d:DesignWidth="800">

    <!-- Вертикальная прокрутка -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding CompletedTrips}" Margin="20">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- Элемент поездки -->
                    <Border BorderBrush="#E0E0E0" 
                            BorderThickness="0,0,0,1" 
                            Margin="0,0,0,10"
                            Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Направление -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding FromCity}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ToCity}" Foreground="#666666"/>
                            </StackPanel>

                            <!-- Дата и время -->
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding DepartureTime, StringFormat='dd.MM.yyyy HH:mm'}"
                                       VerticalAlignment="Center"/>

                            <!-- Места -->
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding AvailableSeats}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>

                            <!-- Водитель -->
                            <TextBlock Grid.Column="3" 
                                       Text="{Binding DriverName}"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>
                            
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>